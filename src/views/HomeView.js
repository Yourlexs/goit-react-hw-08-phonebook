const styles = {
  title: {
    fontWeight: 500,
    fontSize: 48,
    textAlign: 'center',
  },
};

const HomeView = () => (
  <div>
    <h1 style={styles.title}>Hello and Welcome!</h1>
    <p>Here you can store contacts</p>
  </div>
);

export default HomeView;
